#ifdef __IMPORTED__
    #error This file must be #included.
#endif

#include "version.h.acs"

strict namespace Environment
{
    // If `DevelopmentPlayerCap()` is `true`, this cap is enforced and players joining beyond the cap are kicked out.
    #define DEV_PLAYERCAP_COUNT 8

    extern bool IsDevelopment();
    extern bool IsProduction();
}