#ifdef __IMPORTED__
    #error This file must be #included.
#endif

strict namespace Game
{
    internal bool HandlePlayerChatMessage(int id, str message)
    {
        // For now the function only prevents bots from sending a message when they are a zombie.
        return !PlayerIsBot(id) || Player::GetPlayerIsHuman(id);
    }
}
