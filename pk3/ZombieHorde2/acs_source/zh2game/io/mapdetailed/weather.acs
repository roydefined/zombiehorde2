#ifdef __IMPORTED__
    #error This file must be #included.
#endif

strict namespace IO
{
    internal bool ParseWeatherProfiles(int handle, str mapLump, str identifyingSymbol)
    {
        return IO::ParseWeatherProfile(handle, identifyingSymbol, "weatherprofiles", false);
    }
}