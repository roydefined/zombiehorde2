#ifdef __IMPORTED__
    #error This file must be #included.
#endif

strict namespace Event
{
    void ReceiveRandomizedWeatherTrigger(str packet)
    {
        packet = Packet::Read(packet);
        int index = Packet::Value;

        SetReceiveData(0, index);
    }
}