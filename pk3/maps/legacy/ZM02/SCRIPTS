#pragma raw include on
#pragma raw define on

#import "zcommon.bcs"
#import "acs_source/bcsutils.acs"

#include "acs_source/zh2lib.h.acs"
#include "acs_source/zh2game.h.acs"

strict namespace
{
	script "zm02_open" open // Level Setups
	{
		Sector_SetRotation(89, 90, 0);
	}

	Script 2 Enter
	{
		GiveInventory(FLARE_ITEM, 1);
	}

	Script 3 (void) // Falling in canyon
	{
		Thing_Damage(0, 500, MOD_FALLING);
		if(Player::GetIsZombie())
			Teleport_NoFog(96);
	}
}
