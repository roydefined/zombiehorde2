#pragma raw include on
#pragma raw define on

#import "zcommon.bcs"
#import "acs_source/bcsutils.acs"

#include "acs_source/zh2lib.h.acs"
#include "acs_source/zh2game.h.acs"

strict namespace
{
    Script "zm08_open"
    {
        Sector_ChangeFlags(232, SECF_NOFALLINGDAMAGE, 0);
        Sector_ChangeFlags(1, SECF_NOFALLINGDAMAGE, 0);
        Sector_ChangeFlags(61, SECF_NOFALLINGDAMAGE, 0);
    }

    Script 3 (void)
    {
        if(GetActorZ(ActivatorTID()) >= 0.0 && GetActorZ(ActivatorTID()) < 216.0)
        {
            SetPlayerProperty(0, 1, PROP_FLY);
            ThrustThingZ(0, 16, 0, 0);
        }
        else
        {
            SetPlayerProperty(0, 0, PROP_FLY);
        }
    }

    Script 4 (void)
    {
        SetPlayerProperty(0, 0, PROP_FLY);
    }

    Script 5 (void)
    {
        if(GetActorZ(ActivatorTID()) >= 0.0 && GetActorZ(ActivatorTID()) < 24.0)
        {
            SetPlayerProperty(0, 1, PROP_FLY);
            ThrustThingZ(0, 16, 0, 0);
        }
        else
        {
            SetPlayerProperty(0, 0, PROP_FLY);
        }
    }

    Script 6 (void)
    {
        SetPlayerProperty(0, 0, PROP_FLY);
    }

    Int Pillars = 0;


    Script 7 open//water color changer for sectors
    {
        Sector_SetColor(71,25,25,200);
    }

    /*
    Script 8 (Int Sector, Int TerminalNumber)
    {
        Pillars++;
        Print(n:0,s:" activated pillar ",d:TerminalNumber);
        Floor_LowerToLowest(Sector,1);
        TagWait(Sector);
        Delay(35);
        If(Pillars == 4)
        {
            Print(s:"All pillars are activated!");
            Door_Open(88,25);
        }
    }

    Script 9 (void)
    {
        If(CheckInventory("Blueskull"))
        {
            Sector_ChangeFlags(98, SECF_NOFALLINGDAMAGE, 0);
            ClearLineSpecial();
            Print(n:0,s:" pressed a button");
            Delay(Random(35,1150));
            Radius_Quake(1,400,0,1,0);
            Floor_LowerToLowest(85,25);
            TagWait(85);
            Floor_LowerByValue(85,128,1024);
            Floor_LowerByValue(87,128,1024);
        }
    }

    //TeleportInSector(22, 23, 22, 0, 0

    bool Teleporter[BCSUtils::MAX_PLAYERS];

    Script 10 (void)
    {
        Teleporter[PlayerNumber()] = 1;
        for(int x=0;x<=BCSUtils::MAX_PLAYERS;x++)
        {
            if(Teleporter[x])
                SetActorPosition(PLAYER_TID+x,GetActorX(PLAYER_TID+x)+0.0,GetActorY(PLAYER_TID+x)+1440.0,GetActorZ(PLAYER_TID+x)+128.0,0);
        }
    }

    */
}
