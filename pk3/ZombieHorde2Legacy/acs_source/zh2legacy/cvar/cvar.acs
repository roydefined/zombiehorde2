#ifdef __IMPORTED__
    #error This file must be #included.
#endif

// The actual cvar list lives in cvar.shared.acs, #included from cvar.h.acs instead of from here.
// This file is kept around (empty) so the module still has the usual .h.acs + .acs pair.

strict namespace CVar
{
}
