Map improvements
------------------------------------------------

 * * Ladder fixes * *
A lot of maps had ladders that were not working. This apparently happened with the release of Zandronum 3.0, and it affected ladders that did not have an actual midtexture to bump into.
This has been fixed by changing these to properly collide with said ladders so the user can climb them.
The following maps have been changed:
ZE04, ZE19, ZM08M, ZM14, ZM18, ZM21 & ZM25

 * * Crusher fixes * *
A lot of maps had crushers that were not working. The crushers stopped working in ZH2 due to conversion changing the crusher type as Hexen format maps used a different system.
This has been fixed by correcting the crusher type. Note that an additional change has been made on most crushers to ensure that they do not perform excessive damage.
The reduction of the damage on crushers is an attempt to reduce insta-killing factors on maps. To support this change various crushers now have added tag waiting to ensure they do not break.
The following maps have been changed:
ZE02, ZE02B, ZE05M, ZE06, ZE06M, ZE08M, ZE11, ZE12, ZE15, ZE16, ZE17, ZE18, ZE19, ZE21, ZE22, ZE23, ZE25, ZE26, ZE27, ZE29 & ZE30

 * * Music fixes * *
Due to an oversight there were many maps still playing music when the round ended. This was caused by a music function being called which was different that the one initially replaced.
These maps have been changed to now call the music properly.
The following maps have been changed:
ZE15, ZE19, ZE25, ZE29 & ZM15

 * * player spawn fixes * *
Many maps only had 32 player spawns instead of the possible 64, and this put them at risk of introducing telefragging.
These maps now implement all 64 player spawns to prevent this.
The following maps have been changed:
ZE14, ZE15, ZE16, ZE25, ZE30, ZM08, ZM11, ZM13, ZM15 & ZM17

 * * Zombie spawn fixes * *
Some maps did not have many zombie spawns, and this put them at risk of introducing telefragging.
These maps now have more zombie spawns to prevent this.
The following maps have been changed:
ZE14 & ZE30

Other mod changes
------------------------------------------------

- Fix wrong vertical offset on the sniper's zoom meter.
- Removed an insta-killing floor on ZE25 at the start of the map.
- Fixed scrolling rocks on ZE03M.
- Fixed an issue where the "Random map" vote option would pick played maps even though configuration forbids it.
- Fixed ZE12's music not working.
- Fix hud scratching offset not being set properly.
- Fixed an unintended shortcut on the hill at the start of ZE02B
- Map vote threshold now includes the actual percentage of `sv_zh2_playervotepercentage` instead of just checking if it's higher.
- Fixed admin time speedup not working as intended.
- The fire at the end of ZE06M now sets zombies on fire.
- The fake pistol that a lot of maps use now correctly give a handgun when picked up,
- Changed formatting of ZE13's gem resist to look the same as it did in ZH1.
- ZE02B's fire now depletes when the second resist ends.
- Improved the brightness of the underwater section in ZE13.
- Improved the brightness of various places in ZE25.
- The shotgun now always reloads its shell before you can fire again.
- The ally translucency feature has its default configuration reduced.
- The admin menu now correctly takes kevlar from players when the action is given.
- Fixed an issue with kevlar being taken on map change.
- Fixed an issue where the red overlay of a countdown would persist very slightly.
- The hud scaling and hud shaking configuration is no longer configurable and instead hardcoded for the time being.
- Do not shake the pregame state label and round timer when firing.
- Improve syncing of players on map start.