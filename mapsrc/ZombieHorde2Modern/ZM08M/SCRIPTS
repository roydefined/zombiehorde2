#pragma raw include on
#pragma raw define on

#import "zcommon.bcs"
#import "acs_source/bcsutils.acs"

#include "acs_source/zh2lib.h.acs"
#include "acs_source/zh2game.h.acs"

script 2 OPEN//far off bombing
{
        delay (random(35, 2048));
        AmbientSound("sound/b52ambience", 255);
        delay(160);
        AmbientSound("world/volcano1", random(64, 255));
        Radius_Quake2 (70, 32, 30, 0, 2048, 0);
        delay(30);
        Radius_Quake2 (70, 16, 30, 0, 2048, 0);
        delay(30);
        Radius_Quake2 (70, 8, 30, 0, 2048, 0);
        restart;
}

script 3 OPEN//m16 ambience
{
	delay (random(35,256));
	AmbientSound("sound/m16ambience", 255);
	restart;
}

script 4 OPEN//ak47 ambience
{
	delay (random(35,256));
	AmbientSound("sound/ak47ambience", 255);
	restart;
}