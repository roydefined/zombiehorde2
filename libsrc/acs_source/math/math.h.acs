#ifdef __IMPORTED__
	#error This file must be #included.
#endif

strict namespace Math
{
	extern fixed FRandom(fixed min, fixed max);
    extern void RotateVector3D(fixed x, fixed y, fixed z, fixed angle, fixed pitch);
}