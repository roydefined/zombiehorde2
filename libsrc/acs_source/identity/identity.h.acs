#ifdef __IMPORTED__
	#error This file must be #included.
#endif

#define PlayerTIDBase 600

strict namespace Identity
{
	extern void InitializeActivator();
	extern void DisposeTID(int tid);
	extern bool IsValidTID(int tid);
	extern bool IsValidID(int id);
	extern int GetTID(int id);
	extern int GetID(int tid);
}